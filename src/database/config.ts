const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "abc12345678$",
        database: "mynode",
    },
    port: 3000,
    listPerPage: 100,
    tables: {
        employee:"employee"
    }
};

export default config;